{% extends app.request.isXmlHttpRequest ? "layout_ajax.html.twig" : "layout.html.twig" %}

{% block js %}
{% endblock %}

{% block body %}
    <div class="jumbotron form-horizontal">
        <div class="form-group">
			<div class="col-sm-12 text-center">
				<h3>{{ user.prefix|raw }}{{ user.login }}{{ user.suffix|raw }}</h3>
				<img class="img-responsive center-block avatar" src="{{ user.avatar }}" />
				<br>
				<img src="{{ user.image }}" />
				<br>
			</div>
			<div class="col-sm-12 text-center">
				<h2>Obowiązki:</h2>
			</div>
			<div class="col-sm-2"></div>
            <div class="col-sm-10">
                <h4>{{ user.responsibilities|nl2br }}</h4>
            </div>
        </div>
    </div>
{% endblock %}