{% extends app.request.isXmlHttpRequest ? "layout_ajax.html.twig" : "layout.html.twig" %}

{% block js %}
{% endblock %}

{% block body %}
	<div class="page-header">
		<h1>Lista Użytkowników</h1>
	</div>
    <div class="jumbotron">
		<br>
		<div class="list-group">
			{% for id, user in users %} 
			<a class="list-group-item" href="javascript:void(0)" onclick='loadpage("{{ path('usersedit', {id:user.id}) }}");'><span class="badge"><span class="glyphicon glyphicon-wrench"></span></span><span class="badge">{{ id + 1 }}</span>{{ user.login }}</a>
			{% endfor %}
		</div>
		<button class="btn btn-success btn-block" onclick='loadpage("{{ path('useradd') }}");'><span class="glyphicon glyphicon-plus-sign"></span> Dodaj Użytkownika</button><br/>
		<button class="btn btn-primary btn-block" onclick='loadpage("{{ path('admin') }}");'><span class="glyphicon glyphicon-arrow-left"></span> Wróć do Panelu Administracyjnego</button>
    </div>
{% endblock %}